package main

//import "fmt"  // 系统的库
//import "tree" // 自己的库

func main() {

	// 刚安装完go 的时候 GOPATH 是不会被设置的
	// 如果不设置 就会默认在本地的 用户目录下 go
	// 建议 自己设置一下 GOPATH

	// 官方推荐所有的项目 和第三方库都在同一个 GOPATH 下
	// 也可以将每个项目放在不同的 GOPATH 下

	// go fmt 格式化我们的文件
	// go imports  整理import的工具

	// go get golang.org/x/tools/cmd/goimports 获取第三方库

	//go build 来编译
	//go install 产生 pkg 和可以执行文件
	//go run 直接编译运行

}
